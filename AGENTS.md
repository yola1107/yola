# AI 开发指南

本文件适用于 AI Agent 在本仓库中的读取、修改、评审和命令执行。

“必须／须／禁止／不得”为硬性要求；“应／优先”为默认做法，偏离时说明事实依据。

### 绝对红线（高风险）

- **授权**：未经明确授权，不得执行 `git commit`、`git push` 或发布；不得扩大已有授权的目标、范围或操作。
- **工作树**：只修改任务相关内容；不得覆盖、回退或整理用户已有改动。
- **标识符**：未经任务明确要求，不得改变协议、配置、路由和用户指定标识符的拼写、大小写或缩写。
- **生成文件**：不得手改文件头标有 `Code generated`、`auto-generated` 或 `DO NOT EDIT` 的生成文件。
- **SQL**：SQL 值必须参数化。
- **Shell**：禁止将外部输入拼接进 shell 命令；外部参数须经业务白名单校验后，通过参数列表传入。
- **TLS**：生产代码禁止 `InsecureSkipVerify: true`；仅负向测试允许。
- **凭据**：发现明文密钥、Token 或密码时，先报告位置和风险；不得复制、扩散凭据或擅自修改相关逻辑。
- **验证结果**：不得将未运行或部分完成的检查表述为通过。
- **规则边界**：Skill 服从用户指令和适用仓库规则；不得因其措辞、工具偏好或便利性提高优先级、扩大授权、增设审批，或放宽安全、契约、验证、审批和权限边界。

### 范围与授权

- 从仓库根到目标目录读取适用指令；同目录优先 `AGENTS.override.md`，否则读 `AGENTS.md`。更近目录仅覆盖冲突规则，其余上层规则继续适用。
- 沿用会话中有效的任务、约束和授权；评审、诊断、解释和规划默认只读。实施请求须完成修改、必需验证和复审，不停留于方案或承诺。
- 已授权的任务内可逆、非破坏性动作直接执行；仅在适用规则明确要求再次审批时确认。
- 可查证的信息自行查证，常规选择遵循现有约定；仅在无法合理推定且实质影响目标、契约或授权范围时提问。
- 需要新增授权，或冲突无法按适用优先级解决且需用户决定时，仅暂停相关动作；在已有授权内准备可复核结果，说明依据、影响和所需决定。
- 因规则暂停或偏离用户要求时，须给出来源链接、规则原文和适用依据，区分明确要求与解释。
- 等待决定时，仅继续不依赖该决定的已授权工作；不得将未回复视为同意。
- 可选 Skill 或工具不可用时，在相同权限内采用满足同等契约和验证要求的替代方法。

### 执行流程

- 实施前检查 `git status --short` 和相关 diff。
- 结合代码、调用链、测试和依赖，查明行为、状态所有者、验收标准及验证方式。
- 修改前按 [文档索引](docs/README.md) 核对相关契约，按需读取。
- 规划须标明状态；历史或归档计划仅在用户要求继续时执行，不替代当前任务。
- 跨职责或大批量改动按职责边界、迁移顺序和验证方式分批；不得按文件数量拆散原子改动。
- 代码事实推翻方案前提时，重新核对目标和边界，在已有授权内调整实施。

### 设计与改动准入

- 按“正确性与契约 > 状态与生命周期 > 职责与依赖 > 清晰 > 简单 > 行数与 diff 大小”决策。
- 每份可变状态只有一个权威来源；其所有者负责校验、修改和并发保护。
- 明确资源的创建者、失败回滚和关闭顺序；由启动方负责停止后台任务。
- 保持依赖单向、显式；adapter 不得持有业务状态或重新定义业务语义，可管理契约所需的连接、订阅等资源；边界类型转换不得承载业务决策。
- 跨边界只传职责所需的数据和能力；不得泄漏锁、队列、存储模型或无关第三方类型。
- 仅在职责需独立变化、替换实现或隔离测试时新增边界。
- 功能改动须闭合主流程、失败语义、状态和资源生命周期，并同步相关调用方、测试和文档。
- 重构须保持行为等价并有明确净收益；优先删除、合并和复用，不得为指标、形式分层或未来需求新增抽象。
- 只同步清理直接相关、行为等价且低风险的问题；范围外问题只报告，不因存量问题扩大任务。
- 不得交付空实现、临时双轨、无完成条件的 TODO 或无独立职责的纯转交层。
- 性能优化须有 benchmark、profile、监控指标或算法复杂度依据；区分理论收益与实测结果。

### Go 代码

- 按主流程在前、辅助细节在后的顺序组织代码；错误和边界分支优先尽早退出。
- 删除终止分支后的 `else`；扩大作用域、重复求值或损害成对分支可读性时可保留。
- 行为等价重构须保持求值次数与顺序、短路条件、锁保护范围、`defer` 语义与资源关闭顺序、error identity、`nil`、零值、空集合语义及外部 I/O 时机。
- 删除任务内死代码、冗余判断和无语义的中转变量，行为等价地合并重复逻辑；保留有助于表达业务概念或简化表达式的变量，不按使用次数机械内联。
- 提取或保留小函数须有真实复用、独立职责或隔离测试价值；单点调用、无独立语义且内联后更清晰时优先内联。
- 不得为减少行数引入复杂表达式、helper 或抽象。
- 仅在 `defer` 修改返回值或显著增强多返回值语义时使用命名返回值；使用显式返回，避免裸 `return`。
- 用准确、统一的业务名称表达对象和角色；局部名称简短且不丢语义，必要时写明状态、生命周期或单位；不得用泛化名称或自造缩写替代业务名称。
- 命名清理默认限于局部标识符；跨文件或公共 API 改名须由任务明确要求，并先核对引用、接口实现和协议影响。
- 新增字段前确认使用方、生命周期和职责；不得预留未来字段，或重复维护可直接推导且无独立语义的状态。
- 缓存须有性能依据、明确所有者及有效期或失效条件；遵守业务契约中的缓存限制。
- 通过构造参数注入需替换、隔离测试或管理生命周期的外部依赖；避免可变全局状态、隐式初始化和远距离副作用。
- error 必须处理或向上传递并保留错误链；用 `errors.Is/As` 分类，不依赖错误文本。仅在有依据可安全忽略时使用 `_ =`，非显然原因须注释。
- 注释使用简洁中文，只解释契约、原因、不变量、所有权和非显然时序；Go doc 以声明名开头，保留技术术语和工具标记。
- import 默认使用包原名；别名仅用于包名冲突、路径末段与包名不一致，或区分 proto 域与版本；格式与分组遵循 `.golangci.yml`。
- 新增第三方生产依赖前，确认标准库和现有依赖不能合理满足，并评估维护、安全和生命周期成本。

### 验证

按风险完成下表适用的必需检查，将 `<...>` 替换为实际文件、包、目录或基线。仅文档或指令改动时，核对引用、命令和约束一致性。

| 触发条件 | 执行目录 | 命令 |
| --- | --- | --- |
| Go 改动：格式化修改文件 | 仓库根 | `golangci-lint fmt --config .golangci.yml <修改文件>` |
| Go 改动：受影响包测试 | 所属 module | `go test <受影响包>` |
| Go 改动：lint | 仓库根 | `make lint` |
| 公共 API、依赖、跨 module 或多包改动 | 仓库根 | `make check` |
| 并发、连接或生命周期改动 | 所属 module | `go test -race <受影响包>` |
| 根 `api/**` 协议改动 | 仓库根 | `make breaking 'BUF_BREAKING_AGAINST=.git#ref=<基线>'` |
| 服务入口或构建链改动 | 对应的仓库根、`test/ludo` 或 `test/whot` | `make build` |
| 无对应 build 目标的入口改动 | 所属 module | `go build -o <输出目录>/ <入口包>` |

- 使用已安装工具完成常规验证；版本影响结果时记录版本。
- 运行不熟悉或可能依赖外部环境的测试前，核对 `TestMain`、环境变量和依赖条件；外部条件未确认时，不运行全量测试。真实 Redis/etcd 测试遵守 [隔离要求](docs/README.md#开发与验证)。
- Bug 修复优先通过测试稳定复现问题并验证修复；新增测试只覆盖验收标准和直接风险，不复述实现。
- 修改影响已有验证结论时，重跑对应检查；仅复用对应代码、依赖、配置、工具和环境未变的有效结果，不以文档中的历史记录证明当前通过。
- 存在失败或具体未决风险时，补充针对性验证。
- 必需检查通过后，仅因新修改、失败或具体未决风险追加或扩大验证。
- 不重复 `make check` 已覆盖的同配置检查；不得以它替代 `make lint`、race 或协议兼容检查。
- 仅将 `make lint` 用于 AI 和本地检查；不得接入 `make check`、`make all`、CI、Git hooks 或自动化 commit/push 门禁。
- 分别报告新增和存量 lint 告警；不得通过关闭 linter、扩大 exclusion 或无关重构规避。
- 复杂度告警仅用于定位热点；不得为清零改变契约、新增抽象或机械拆分。
- 检查失败或无法运行时，报告原因、替代证据、剩余风险及未完成项。

### 工具与生成

| 操作 | 适用范围 |
| --- | --- |
| `make init` | 仅用于初始化或明确升级；通过 `go install ...@latest` 更新工具。 |
| 根 `make all` | 仅在任务需要根 module 完整生成时运行；生成并整理其依赖，再检查两个 module，不覆盖 `test` module 的生成。 |
| Ludo/Whot 生成 | 按源定义选用各自 Makefile 的生成目标。 |

- 按 [开发文档](docs/README.md#开发与验证) 和对应 Makefile 核对命令定义与生成范围；生成前确认源定义和目标，生成后完成适用验证。

### Git 与交付

- 提交或交付前复审完整 diff、相关调用链和任务内同类问题；不得以测试通过替代复审。
- 提交或交付前检查 `git status --short`、`git diff --check` 和 `git diff --cached --check`。
- 提交获授权后，仅暂存和提交复审过的任务改动；不得混入无关内容，包括已有暂存内容。
- 每个提交只含一个原子改动；生成文件与对应源文件一起提交。
- 使用 `<type>(<scope>): <中文摘要>`；type 限于 `feat|fix|refactor|docs|test|chore`。
- 使用简洁中文沟通，首句给结论，保留技术术语。
- 按任务需要交付结果与关键依据；实施任务说明已运行或复用的验证、未运行项和剩余风险，不复述执行过程。
- 区分事实与推测；代码结论以 `path:line` 提供证据。

### 指令文档格式

- 维护 `AGENTS.md` 时，用 Markdown 三级标题拆分规则；绝对红线置于首个模块，使用粗体标签列表。
- 核心规则优先使用表格或 Bullet Points；使用指令式短句，单段不超过 3 行。
- 保留动作、禁止事项和必要条件，删除重复规则和解释性赘述。
